<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidBootcamp2025Frontend" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowSplashScreenBackground">@color/green</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_img</item>
    </style>
    <style name="CustomDatePickerDialog" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <item name="android:background">@drawable/custom_dialog_background</item>
        <item name="android:textColorPrimary">@color/backgroundPrimary</item>
        <item name="android:textColorSecondary">@color/backgroundPrimary</item>
    </style>


</resources>